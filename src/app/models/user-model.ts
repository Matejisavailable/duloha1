export class User{

  id: string;
  name: string;
  contact: string;


  constructor(id: string, name: string, contact: string) {
    this.id = id;
    this.name = name;
    this.contact = contact;
  }
}
